# See http://kvz.io/blog/2010/08/11/haproxy-logging/
# See http://serverfault.com/questions/214312/how-to-keep-haproxy-log-messages-out-of-var-log-syslog
<% if @udp_listen -%>
$ModLoad imudp
$UDPServerAddress <%= @udp_ip %>
$UDPServerRun <%= @udp_port %>
<% end -%>

if $programname == '<%= @programname %>' then <%= @log_dst %>
& ~
# & ~ means not to put what matched in the above line anywhere else for the rest of the rules
